function openPurse() {
    document.getElementById('MyPurse').alt = "open purse";
    // add in the images later
}

// add a function that closes the purse later if it's opened and clicked again.
// add a function that allows you to zoom in to each item in the purse when clicked